const express = require("express");
const app = express(); // 创建一个express应用

// 处理api请求
app.use("/api/admin",)


const port = 5001;
app.listen(port, () => {
    console.log(`server listen on ${port}`);
})